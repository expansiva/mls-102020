{
  "$id": "https://collab.codes/schemas/agentImproveMolecule2/i5-playground.schema.json",
  "version": 1,
  "type": "object",
  "additionalProperties": false,
  "required": ["edits"],
  "properties": {
    "edits": {
      "type": "array",
      "minItems": 1,
      "description": "Targeted edits to the playground page, applied in this order.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["op", "content", "why"],
        "properties": {
          "op": {
            "type": "string",
            "enum": ["replace", "append", "create"],
            "description": "replace: swap an exact block. append: add at the end. create: write the whole page, only when it does not exist yet."
          },
          "find": {
            "type": "string",
            "description": "Required by replace: the exact current text, copied verbatim, occurring EXACTLY ONCE in the page."
          },
          "content": { "type": "string", "description": "The replacement text, the appended block, or the whole page on create." },
          "why": { "type": "string", "minLength": 3, "description": "One short line, in the user's language." }
        }
      }
    }
  }
}
