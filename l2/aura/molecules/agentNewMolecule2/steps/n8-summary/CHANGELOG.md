# n8-summary — CHANGELOG

## 2026-07-29 — created (control item 3.9)

Reads the `n6-done` and `n7-done` result payloads to know whether the demo and the group index actually
came out (both are allowed to fail with `ok:false`), so the summary states the gaps instead of letting
the user discover them later. Mentions the Variant as the next step when the molecule is themed.
