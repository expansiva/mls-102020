# v6-summary

Cheap final summary in the user's language (modelType general).

Input: context.json + on-disk outcomes (html exists? coldStart?). Output: short prose summary; completes the task.
Cold start: recommends visual review (pilot) + registering the molecule in theme.ts examples after approval.
