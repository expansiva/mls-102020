{
  "flowId": "agentChooseMolecules2",
  "schemaVersion": "2026-09-08-choosemolecules2-v2-prose",
  "specFirst": "Written BEFORE the code, per skills/agentsBestPractices.md §1. This file, spec.md and README.md are the design record. The plan was approved by the product owner on 2026-09-02 (session plan: binary-purring-lobster) and is restated here; when behaviour must change, change this file first, then the code. AMENDED 2026-09-08 for the page defs format change (v2, see decisions.definitionFormat) — approved in session by the product owner, who chose both open questions: regions come from the PROSE alone (probe-style), and the run writes the pipeline only, leaving `export const definition` byte for byte intact.",
  "description": "Given an explicit catalogProject and the reference of an existing page .defs.ts, decides which molecule (group + tag) serves each region of that page and rewrites ONLY that file's `pipeline` — the chosen molecules' source files appended to pipeline[0].dependsFiles and their groups' usage contracts to pipeline[0].skills. `export const definition` is READ (it is the description the choice reasons over) and never written. Writes nothing else: no report, no l4 artifact, no trace file. A sibling of agentChooseMolecules (the probe that only measures), built to actually equip a page.",
  "principles": [
    "ZERO ARTIFACT RULE. The only file this agent ever writes is the target .defs.ts, and inside it only the `pipeline` value (steps/c3-patch). Every handoff between steps travels through the task tree's own step `result` field (helpers/cm2Types.cm2ReadC1Result / cm2ReadGroupResult) — never a file. No report.json, no c1-groups.json, no per-attempt trace, no l4 folder anywhere, in any project.",
    "THE DEFINITION IS READ-ONLY. `export const definition` is prose (see decisions.definitionFormat) and is the description c1 reasons over. It is spliced back byte for byte: the only cut this agent makes is around the `pipeline` value. A run can never change what the page says it is.",
    "EXPLICIT CATALOG, NO DISCOVERY. catalogProject arrives in the mention argument. There is no dependency search, no chChooseCatalog, no 'more than one catalog reachable' ambiguity — all of that existed in the probe only because it never received the project.",
    "REGIONS COME FROM THE PROSE, AND ONLY FROM IT. c1-groups reads the target's `definition` text and names the regions itself, exactly as the probe does — the deterministic extraction from dataBindings[]/inputs[] is gone with the object it used to walk. The prompt's anti-invention half carries the weight now: a field the prose does not name is never a region (steps/c1-groups/prompt.md, inherited from the probe's own measured rules).",
    "THE CATALOG IS THE ONLY SOURCE OF TAGS. Same anti-invention gate as agentChooseMolecules, imported unchanged from its steps/c1-groups/gate.ts and steps/c2-molecules/gate.ts — a tag outside the group's published list is refused by a deterministic gate, never completed by code.",
    "ONE CATALOG LEVEL PER PROMPT. c1 sees level 1 (the group list); each c2 sees level 2 of ONE group. Same funnel and the same reason as the probe (flow.json.principles there): the whole catalog does not fit a prompt.",
    "PERSISTED SHAPE IS MINIMAL, AND IT IS THE PIPELINE. Two flat arrays of file references — no reason, no scenarioUsed, no region name, no gate history. Those exist transiently inside the run and are never written anywhere. What reaches the render model is what materialize reads: dependsFiles as context sections, skills concatenated into its system prompt."
  ],
  "decisions": {
    "argument": "The whole mention is one JSON object, '{\"catalogProject\": N, \"target\": \"...\"}', parsed with plain JSON.parse (helpers/cm2Entry.cm2ParseEntry) — the same choice agentChangeFrontend's 'only-materialize' command already made for a single flat-object argument. Both keys required; no free prose, no optional-prefix-plus-prose shape (unlike agentChooseMolecules's chParseEntry), so there is no reason to reach for mls.common.safeParseArgs or hand-rolled brace-matching.",
    "bootstrap": "Deterministic (skipRootLLM: true on the initial add-message-ai), same gesture as agentChangeFrontend.ts's beforePromptImplicit. There is no c0-classify: nothing here needs classifying, and the run does not want to spend a call on it.",
    "regionGranularity": "v2 (2026-09-08): the four DETERMINISTIC kinds (surface / entry / trigger / page) are gone with helpers/cm2Regions.ts, which walked a dataBindings[] the file no longer has. Regions are named by c1 from the prose, so the granularity rules are the probe's own — steps/c1-groups/prompt.md now carries them verbatim, including the measured anti-superdecomposition half (a verb a component performs on its own content is not a region of its own) and the anti-invention half (never a field the prose does not name), which is what keeps a four-line description from turning into a guessed form. WHAT IS LOST, stated plainly: the trigger of a command with no typed field and the page-wide feedback surface were emitted deterministically before and now depend on the prose naming them, which it usually does not; and the region id is no longer a write-back address, so nothing downstream joins on it — it is a label for the summary and the c1→c2 join, nothing more. HISTORICAL, for the record: the deterministic extraction yielded 10 regions on _102046_ clientCatalogue/page21 against the probe's 12.",
    "patchTarget": "The SAME file passed in target — page11, page12 or any future genome sharing the { definition, pipeline } shape. This agent does not decide which genome gets molecules; whoever calls it decides which file to point at.",
    "modelType": "reasoning + x-tool-strict on both LLM steps (c1-groups, c2-molecules), per skills/modelTypes.md and the agentChooseMolecules precedent. No classifier model anywhere.",
    "retry": "One retry with the gate errors in the prompt (CM2_MAX_ATTEMPTS = 2), same budget as the probe. A c2 that still fails after 2 attempts records ok:false and completes — c3-patch leaves that group's regions untouched rather than failing the whole run. FIXED 2026-09-08: the c2 retry step used to be planted without `target`, so the second attempt lost both the page context and the project language — the tie-breakers the first attempt had. The retry prompt now carries target, like c1's always did.",
    "idempotency": "c3-patch compares the rewritten source to the original before writing; a rerun that changes nothing touches no file at all. A region already annotated is reconciled (overwritten or removed), never duplicated.",
    "projectContext": "2026-09 addendum, measured on _102046_/approveChangeOrder: c2 (not c1 — group choice does not need locale) also reads the TARGET project's own l5/project.json (helpers/cm2ProjectContext.ts) for its declared language(s), added as a 'Project context' prompt section. This is what lets c2 pick between locale-bound siblings (e.g. groupEnterMoney's en-US vs pt-BR molecules) instead of correctly-but-avoidably answering 'none' for lack of any signal. Still a DECLARED fact, never an inference — same category as the contract's own field types, just sourced from l5. Absent/unreadable l5/project.json -> the section is omitted, never padded with a default.",
    "pageContextC1": "v2 (2026-09-08): c1 receives the whole prose `definition` as its human prompt, so there is no separate {{pageContext}} block for it any more — the prose IS the page context. c2, which never sees the definition, still gets it as a section: helpers/cm2PageContext now parses the four labelled prose lines instead of purpose/presentation.categoryRef/pageObjective, and `uxExperience` (processWizard, entityRecordManagement, dashboardCommandCenter, ...) is the declared experience shape that took over from presentation.categoryRef in the c2 prompt's 'the catch-all row is the trap' step. 2026-09-04, SUPERSEDES the 'c1 was left alone' decision below: c1 now gets {{pageContext}} too. The earlier reasoning was that c1 had no measured defect — but comparing against the probe on clientCatalogue/page21 showed it does: c1 sent the client list to groupViewTable while the probe sent it to groupSelectOne (ml-listbox-sidebar-select), and the target's own pageObjective declares 'master-detail list with contextual selection' plus the anti-pattern 'Usar um seletor livre de identificadores para editar ou excluir'. A list whose rows feed a command's selected id is a SELECTOR, and groupSelectOne is where the selector siblings live (it even publishes ml-table-single-select). c1 was not correct, it was correct-looking in isolation.",
    "pageContext": "2026-09-04 addendum, from a measured instability: across two runs of the SAME page, c1 chose the right group every time and c2 flip-flopped inside it — ml-data-table vs ml-view-table (11 siblings), ml-multiline-text vs ml-enter-text (8), ml-radio-group vs nothing (12) — while groupEnterMoney (2 siblings) stayed stable. Instability scaling with sibling count is the signature of missing discriminating evidence, and the catalog was NOT the gap: groupViewTable's skill text ships 10 scenario rows, 11 full descriptions and their layout axes. The gap was the mechanical `need` line, which matched only the catch-all scenario row ('Listagem geral com ordenação e paginação' — which recommends 10 of 11). FIX: c2 now also reads the target page's own declared intent (helpers/cm2PageContext.ts) — purpose, presentation.categoryRef and, on the goal-first genomes, the whole pageObjective (jobToBeDone, primaryDecision, usageFrequency, decisiveInfo, informationHierarchy, antiPatterns, criticalActions[].presentation) — plus one structural fact counted from the defs: how many command inputs are fed by a row selection. ~2.3KB on the measured page, against a 2-6KB group catalog. Read FRESH from the target in c2 rather than threaded through c1's result (which every later step reads). c1 was deliberately LEFT ALONE: it was already 100% correct on both runs, and changing a step with no measured defect is how a fix becomes a regression (agentsBestPractices: 'structural refactors ship with identical external behavior'). page11 has no pageObjective, so there the section degrades to purpose+categoryRef; a defs declaring none of it gets no section at all.",
    "needLine": "2026-09-04: cm2Regions also states, per entry region, the input's declared `required` flag and HOW MANY typed (presentation:'form') fields the same command has — a one-field command is a single decision, a five-field one is a form, and that changes which sibling fits. Both are facts the defs already carries and this agent was discarding. Still nothing inferred from a field name.",
    "referenceForm": "2026-09-04, measured then corrected: TWO reference forms exist and are not interchangeable. IMPORT form ('/_102020_/.../usage' — leading slash, no extension) is what `await import()` takes and what a group's index.defs.ts publishes in `usageContract`. PIPELINE form ('_102020_/.../usage.ts' — no leading slash, with extension) is what pipeline[].skills/dependsFiles take. This agent was writing the import form into the pipeline, and agentCfeMaterializeGen SILENTLY dropped every such entry (readSections/readContextSections skip an empty read), so a usage contract that never reached the model was indistinguishable from one never listed: 4 of 6 skills and 6 of 8 dependsFiles were lost. Every reference this agent puts in a pipeline array now goes through cm2Types.cm2PipelineRef. Target form settled by the platform's own generator, not by inference: across every .defs.ts agentChangeFrontend wrote in a real client project, no pipeline reference carries a leading slash. Consequence for the render skill (genCfePage21RenderTs): the runtime import is now built from the dependsFiles entry by prepending '/' and swapping '.ts' for '.js', not by appending '.js'.",
    "definitionFormat": "2026-09-08 — THE FORMAT CHANGED AND THIS IS v2. A page .defs.ts used to carry `export const definition = { pageId, purpose, presentation, pageObjective, dataBindings[] }` (mls-102046). It now carries a TEMPLATE LITERAL of prose (measured on _102047_/l2/controleChamados, all three genomes page11/page21/page31): four labelled lines — `page:`, `actor:`, `purpose:`, `uxExperience:` — plus a fixed closing paragraph addressed to the render model ('The page extends the shared base class of this workspace... do not list fields and do not list routines'). The structural facts did not vanish, they MOVED to the workspace shared defs (web/shared/<page>.defs.ts still holds dataBindings[] with the same inputs[]/presentation shape, plus i18n column and field labels, scenaries and destructiveCommandIds); the platform's own gates already follow them there (cfeMaterializeCore.pageDefinitionForChecks: 'page11: the workspace shared map... the prose definition has neither'). READING THE SHARED WAS OFFERED AND DECLINED: the product owner chose the prose as the only evidence, probe-style, with the consequence accepted — four lines discriminate less than a typed binding list, so 'none' is expected to appear more often, which is this family's honest answer and not a defect. The shared remains the obvious first move if the choices come back too coarse.",
    "whatIsWritten": "2026-09-08 — THE PIPELINE, AND NOTHING ELSE IN THE FILE. With a prose definition there is no dataBinding or input node left to carry `molecule: { group, tag }`, so the whole annotation path (applyMoleculeChoices, the root pageMolecules[] array and its role vocabulary) is retired with the object it addressed. Nor is a new pipeline KEY an option: PipelineItem (cfeMaterializeCore.ts) is a closed interface and materialize reads exactly two of its fields, so an extra key would be dropped in silence. So the output is the two arrays that do reach the render model: dependsFiles gets the chosen molecules' own .ts files (read back as '## Context files' sections) and skills gets their groups' usage contracts (concatenated into the render system prompt). The region→molecule MAPPING is therefore not persisted: the render model receives the components and the contracts and composes the page from them. Appending the map to the prose was offered — buildHumanPrompt does print `definition` verbatim as '## Definition', so it would have reached the model — and declined: the definition stays intact.",
    "pipelineReconciliation": "2026-09-08 — the pipeline is now the ONLY output, so appending to it is not enough: a rerun whose choice CHANGED would leave the previous molecule in dependsFiles forever and the render model would get two components for one region. c3-patch therefore PRUNES this agent's own entries before adding the current ones, recognized by shape and by shape only — dependsFiles matching l2/molecules/<group>/<name>.ts, skills matching l2/aura/molecules/skills/<group>/usage.ts. Everything the generator put there (web/shared/<page>Dts.txt, designSystem.ts, genCfePage21RenderTs.ts, the collabux template) matches neither pattern and survives untouched. Added entries are SORTED, so a rerun that chose the same set writes nothing even when c1 happened to return the groups in another order — without that, the byte-equality idempotency check would fire on ordering alone.",
    "pageMoleculeShape_retired_v2": "RETIRED 2026-09-08 by decisions.definitionFormat / whatIsWritten. Kept as the record of what v1 did: 2026-09-04: a region serving the WHOLE page has no binding node to carry `molecule`, so it lands in a ROOT array — `pageMolecules: [{ role, group, tag }]` — addressed by the region id `page::<role>` (unambiguous: every binding id this platform generates starts with `binding.`). ARRAY WITH A ROLE, not one field per role (`moleculeFeedback`, `moleculeConfirmation`, ...), because destructive-confirmation and workflow-progress are already known roles and a vocabulary grows without changing the shape; the vocabulary is CLOSED and extended deliberately, holding `feedback` only today. ROOT, not nested under `presentation` (where categoryRef lives): `presentation` is agentChangeFrontend's object, and this agent's output inside it would be exposed to a partial rewrite by its owner — at the root, pageMolecules is unambiguously this agent's, the same clarity of ownership `molecule` has inside a binding, which had no alternative. Safe typewise: `definition` carries no `satisfies` (unlike `pipeline`, which is `as const`). Reconciliation is per role — a rerun replaces its own role and keeps the others; answering none for the last role deletes the key rather than leaving `pageMolecules: []`.",
    "triggerRegion_retired_v2": "RETIRED 2026-09-08 by decisions.definitionFormat / whatIsWritten. Kept as the record of what v1 did: 2026-09-04, measured by running the probe and this agent on the SAME file (_102046_ clientCatalogue/page21): the probe found 12 regions to this agent's 7, and 3 of the 5 it could not see were the buttons executing cmdCreateClient/cmdUpdateClient/cmdDeleteClient — which the probe routed to groupTriggerAction, an entire group unreachable from here. Sharpest case: cmdDeleteClient has NO 'form' input (its only input is the selected id), so the whole command binding produced zero regions and vanished from the answer. A command's own trigger is as much a molecule-serving interaction as any field, and it is fully derivable — every command binding has exactly one. After the fix the same file yields 10 regions, covering 10 of the probe's 12.",
    "typeResolution_retired_v2": "RETIRED 2026-09-08 by decisions.definitionFormat / whatIsWritten. Kept as the record of what v1 did: Field types come from the sibling web/contracts/{page} file: its .defs.ts first (source of truth), a regex fallback over its compiled .ts when the .defs.ts is not on disk (confirmed necessary on a real client project, mls-102046, where only the .ts was present locally). An unresolved type is honestly 'unknown', never guessed from the field name — same rule the catalog's own usage guidance already states."
  },
  "conventions": {
    "target": "An import-style reference to a page .defs.ts, e.g. '_102046_/l2/buildFlowFsm/web/desktop/page11/approveChangeOrder.defs' — decoded by agentChooseMolecules's chFileRefFromImport (reused, not duplicated). Must resolve to extension '.defs.ts'.",
    "contractPath": "RETIRED v2 (2026-09-08): field types came from web/contracts/{page} to build the deterministic regions' need lines. With regions named from prose there is no field to type, so cm2ContractFileFromTarget and both contract parsers are gone. The declared path, if a future version wants it, is published by the workspace shared defs as contractRef.tsPath — no convention to re-derive.",
    "handoff": "c1's result carries { catalogProject, target, regions, groups } forward; each c2's result carries { group, ok, choices, usageContract }. Both are read back via getAllSteps + planId match on the done-anchor (helpers/cm2Types.cm2ReadStepResult) — the exact mechanism agentChooseMolecules's own fan-out already uses to read c1, generalized to every subsequent read in this agent.",
    "writesNothingElse": "No molecule, no usage contract, no group index, no todoFrontend/statusFrontend change, no config.json, no materialization — and, inside the target itself, nothing but the `pipeline` value. Turning the equipped pipeline into rendered markup is agentCfeMaterializeGen's job, out of scope here.",
    "definitionRead": "The prose of `export const definition`, read verbatim between its backticks (helpers/cm2DefsPatch.parsePageDefsSource). A file whose definition is still the v1 JSON OBJECT is refused with an error that says so by name — never parsed on a guess, and never annotated the v1 way."
  },
  "treeOverview": [
    "(root) agentChooseMolecules2 — deterministic bootstrap, no LLM",
    "  c1-groups            the target's prose definition + level 1 -> LLM names the regions and the group of each, or none",
    "  c1r-fanout            the root again: plants one c2 per chosen group, plus c3-patch",
    "    c2-<group>          level 2 of that group -> LLM decides molecule per region (anti-invention gate)",
    "  c3-patch              deterministic: rewrites the target's pipeline — the run's only write"
  ],
  "steps": [
    {
      "planId": "c1-groups",
      "agentName": "agentCm2Groups",
      "model": "reasoning",
      "dependsOn": [],
      "goal": "Extract regions deterministically from the target's dataBindings/inputs (+ sibling contract types) and ask the LLM which published group covers each, or none.",
      "gate": {
        "file": "reused from agentChooseMolecules/steps/c1-groups/gate.ts",
        "onFail": "retry 1 with errors in the prompt; a 2nd failure fails the step"
      },
      "doneAnchor": "c1-done",
      "result": "{ catalogProject, target, regions: ChRegion[], groups: string[] } — read back by c1r-fanout and c3-patch, never written to disk"
    },
    {
      "planId": "c1r-fanout",
      "agentName": "agentChooseMolecules2 (root)",
      "model": "none (deterministic)",
      "dependsOn": [
        "c1-done"
      ],
      "goal": "Read c1's result from the task tree and plant one c2 per DISTINCT group, plus c3-patch depending on all of them. With no group chosen, c3-patch still runs alone.",
      "doneAnchor": "none"
    },
    {
      "planId": "c2-<groupfolder>",
      "agentName": "agentCm2Molecules",
      "model": "reasoning",
      "dependsOn": [],
      "goal": "For ONE group: choose the molecule for each region c1 assigned to it. Never fails the run — a gate that refuses twice records ok:false and completes anyway.",
      "gate": {
        "file": "reused from agentChooseMolecules/steps/c2-molecules/gate.ts (anti-invention, unchanged)",
        "onFail": "retry 1 with errors in the prompt; a 2nd failure records ok:false and completes"
      },
      "doneAnchor": "c2-<groupfolder>-done",
      "result": "{ group, ok, choices: [{region, tag}], usageContract } — never written to disk"
    },
    {
      "planId": "c3-patch",
      "agentName": "agentCm2Patch",
      "model": "none (deterministic)",
      "dependsOn": [
        "every c2-<groupfolder>-done"
      ],
      "goal": "Join c1's regions with every c2 group's accepted choices, patch definition + pipeline, and rewrite the target .defs.ts. Skips the write entirely when nothing changed.",
      "doneAnchor": "none (terminal step)"
    }
  ],
  "artifacts": {
    "the target .defs.ts (its `pipeline` value rewritten in place)": "the ONLY file this agent writes, and `export const definition` is not part of the cut: pipeline[0].dependsFiles gets the chosen molecules' own .ts files and pipeline[0].skills their groups' usage contracts, this agent's own previous entries pruned first and the new ones sorted (decisions.pipelineReconciliation)"
  },
  "outOfScope": [
    "a molecule-aware render skill that actually composes markup from the equipped pipeline — this agent only equips it",
    "running materialization (agentCfeMaterializeGen) or touching todoFrontend/statusFrontend/config.json",
    "reading the workspace shared defs (web/shared/<page>.defs.ts) for dataBindings[], i18n labels, scenaries or destructiveCommandIds — offered and declined, see decisions.definitionFormat",
    "annotating the v1 JSON-object definition (`molecule: {group, tag}` on a binding, the root pageMolecules[] array) — retired with the format, see decisions.whatIsWritten",
    "creating the target file — the caller decides which file to point this agent at",
    "catalog dependency discovery — the caller must know which project's catalog answers, and pass it explicitly"
  ],
  "knownGaps": {
    "thinEvidence": "v2's accepted cost (decisions.definitionFormat): four labelled prose lines discriminate less than the typed binding list v1 walked, so more regions come back 'none' and the ones that do not are chosen from less. The measurement to run is the same one that produced v1's numbers — this agent and the probe on the same page — and the first move if it reads badly is the shared defs, not a longer prompt.",
    "mappingNotPersisted": "the region→molecule decision is no longer written anywhere (decisions.whatIsWritten): the pipeline carries the chosen components as a flat set, so the render model re-decides which one serves which region. Nothing downstream can audit the choice from the file alone.",
    "destructiveConfirmation": "v1's gap, restated: no published group covers a blocking confirmation dialog, and the probe itself honestly answered null for it. NOTE for whoever revisits this — v1 said it 'cannot be tied to a specific binding deterministically', and that stopped being true when the structure moved: the workspace shared defs publishes `destructiveCommandIds` as a DECLARED list. It is unreachable from the prose alone, so it stays a gap here, but it is no longer an inference anywhere.",
    "deterministicRegionsLost": "a command's trigger (v1 emitted one per command binding, including a command with no typed field) and the page-wide feedback surface (v1's page::feedback) were structural facts before and are now only reachable if the prose names them, which the generated four lines do not. Measured for v1 on _102046_ clientCatalogue/page21: the trigger fix alone was 3 of the 5 regions this agent had been blind to."
  },
  "notBuilt": {}
}