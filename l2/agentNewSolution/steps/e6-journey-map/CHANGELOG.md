# e6-journey-map CHANGELOG

- 2026-07-07 — created (single call + e6-done anchor; schema e6-journey-map v1; gate with
  workspace/operation/workflow/actor/entity resolution, operation coverage, landing checks and
  advisory-edge warnings; moduleName/note attached deterministically after the call).
- 2026-07-08 — fix: defs written with stor extension '.defs.ts' (was '.ts'; files were invisible to Stage 2/3, which filter extension === '.defs.ts').
- 2026-07-08 — fix: gate-failed run with a retry in flight is now completed-with-trace instead of 'failed' ('failed' marks the whole task failed and orphans the retry — msgtask1 evidence).
- 2026-07-08 — interaction cleaner ('input_output') on completed runs with artifacts on disk (DynamoDB 400KB).
- 2026-07-08 — repairE6WorkflowIds: kind='workflow' workspaces without workflowId get it inferred deterministically from the classification (both LLM attempts omitted it in the cafeFlow run — 'workspace.workflow.missing'); prompt reinforced; ambiguity still gates.
- 2026-07-11 — workspace kind canonical + derived (UX research on 102051, ImproveChangeFrontend2 task 1): kind enum gains 'entityManagement' (schema + gate). New deterministic attach deriveE6WorkspaceKinds (runs before repairE6WorkflowIds): workflow-owned operations -> 'workflow'; all-standalone create+update on the workspace entity -> 'entityManagement' (workflowId cleared); otherwise 'operation'. The LLM label is no longer trusted (the 102051 run labeled entity CRUDs as 'workflow', which rejected the list-first CRUD template downstream by construction). Gate: entityManagement workspace declaring a workflowId = error. prompt.md updated (management workspaces are entityManagement, list-first).
- 2026-07-11 — deriveE6WorkspaceKinds refinement (validated against the real 102052 run): auxiliary READ-ONLY operations on another entity (e.g. queryLowStockAlerts on the stockManagement page) no longer demote a management page to 'operation'. Rule: create+update on the WORKSPACE entity + foreign operations restricted to query/view => entityManagement. The 102052 journeys defs were patched accordingly (deterministic output of the fixed rule).
- 2026-07-18 — schema v2 (ns-e6-v2): workspace declares `sections[]` with organisms `{operationId, role, attachTo?}` instead of a flat `operationIds` list (D4 — the productCatalog case where browse+search+filter were 3 sibling lists). `operationIds` is now DERIVED from the organisms by code (source of truth = sections), preserving the changeFrontend consumer and e7 coverage/capability reads. Role enum: primarySurface | filterControl | contextualAction | detailPanel | batchAction | navigationEntry. New deterministic gates: coverage EXACTLY 1 organism per operation (was >= 1 workspace); exactly 1 primarySurface per section; filterControl requires attachTo → same-section surface; detailPanel only getById; batchAction only command over multiple selection or no public input (facts read from operation defs, passed into the gate context — gate stays disk-free). prompt.md rewritten task-oriented (classify into roles). isRecord moved local (was imported from nsFs, which pulled the libStor/libModel DOM chain and made the gate test uninstantiable). filterControl input-field-vs-target match deferred to the D3 contracts step (T7).
- 2026-07-18 — schema v3 (ns-e6-v3): workspace `actor` -> `actors: string[]` (D6). New authorization gate `workspace.actors.notCovering`: workspace.actors must ⊇ ∪ of the hosted operations' actors (navigationEntry links excluded — their actors belong to the owning page). operationFacts gains `actors` (read tolerant of old singular `actor` via helpers/nsActors.readActors). markdown groups by each actor; prompt asks for the actors array.
- 2026-07-18 — projection vocabulary in the prompt (newSolution_11 fix — 3-run A/B). Symptom: every e6 attempt-1 failed the A4.2 `from`-traceability gate because the LLM guessed field names (`searchProducts.petType` vs the real `petTypeId`; `$items.id` vs `productId`; `image` vs `imageUrl`), and the retry then DROPPED the projections entirely (contracts degraded to identity/passthrough — defeating the run-9 page-shaping goal). Root cause: the e6 human prompt only carried operation SUMMARIES (title/kind/story), never the real inputIds/outputShape fields. Fix: each operation summary now includes `inputNames` + `outputPaths` (the latter via the same `collectNsOutputPaths` the gate validates against, so prompt vocabulary == gate vocabulary), and prompt.md instructs the LLM to copy those names verbatim into `from` (never invent/shorten/rename). Closes the loop so projections succeed on attempt 1 instead of degrading on retry.
- 2026-07-18 — schema v5 (ns-e6-v5): bffCalls / BFF view contracts (newSolution_10 N1–N3). The workspace declares `bffCalls[]` (the wire contract: 1..N usecases composed + a projection of only what the page renders); organisms trade `operationId` for `dataSource` (query) / `action` (command) / `attachTo` (filterControl → a query bffCall) / `slice` (which top-level field of a composed call). `operationIds` is now DERIVED from `bffCalls[].uses`. New gates: bffId unique per workspace (bff.id.duplicate, A4.1); `from` traceability — every bffCall input/output `from` resolves to a real inputId / outputShape path of an operation in `uses`, incl. the `$items.<col>` shorthand (bff.input.from.*/bff.output.from.*, A4.2 — operationFacts gains inputNames + outputPaths via collectNsOutputPaths); composed calls (uses N>1) are query-only (bff.command.composed, A4.5); `optional` only on composed uses (bff.optional.notComposed) and a composed call demands a valid `slice` from every consumer (organism.slice.missing/.invalid, A4.5b); free inputs (pagination/flags) declare a `type` when they carry no `from` (bff.input.untyped, A5). Role gates rewired through organism → bffCall.uses → operation. Coverage relaxed from "exactly 1 organism" to A4.4 "≥1 bffCall". N2 default: prepare synthesizes an identity bffCall (bffId==operationId) per organism reference when the LLM declares none — A/B migration stays regression-free (legacy operationId organisms degrade to identity views). N3: route `<module>.<workspaceId>.<bffId>` derived (deriveE6BffRoutes), never declared.
- 2026-07-18 — schema v4 (ns-e6-v4): `kind: 'landing'` + content organisms (D7). Role enum gains hero|banner|richText|imageSet|ctaLink (pure content, NO operationId) + showcase (content fed by a read-only query, operationId required). organism.operationId is now OPTIONAL (omitted for content roles); the gate enforces role-conditional presence (organism.operationId.missing/.unexpected) since JSON required can't. Landing pages skip the exactly-1-primarySurface rule and dispense operations; showcase op must be query/view (showcase.notQuery); content roles are landing-only (content.role.notLanding). 'public' is a pseudo-actor (NS_PUBLIC_ACTOR) exempt from the E4 roster check and the ⊇ actor-union. deriveE6WorkspaceKinds preserves an LLM-declared 'landing'. prompt.md describes landing + content vocabulary. Asset origin (images/banner) deferred (spec open question); ctaLink→workspace target validation deferred.
- 2026-07-19 — newSolution_14 wave (P1–P4): **P1 list/paginated form** — the `$items.<col>` shorthand is now valid ONLY inside an item.fields block (collectNsOutputPaths split into collectNsOutputPathSets {top,item}; NsE6OperationFact.outputPaths → outputTopPaths+outputItemPaths); validateBffOutput checks position (top vs item) so a flat list of item columns at the output top level is INEXPRESSIBLE, plus shape gates `bff.output.paginated.noArray` (paginated needs exactly 1 array field + scalar envelope) and `bff.output.list.hasEnvelope` (list fields are the item columns). No emitter change (it already renders `{arrayField: Item[], total}`). prompt.md documents both shapes with examples. **P2** — the e6 single call sets onFailure='wait_after_prompt' and retries once on an LLM-CALL failure (no payload) before failing with a truthful infra message (task3 died here). **P3** — promptChars logged in the green trace. **P4** — prompt examples: detailPanel in the surface's section; single-row delete is contextualAction not batchAction.
- 2026-07-19 — newSolution_14 P7 (two-phase e6) STRUCTURAL CORE + P8 (permanent journeys). The single-call e6 becomes: PHASE 1 `e6-journey-map` — a LIGHT site-map call (schemas/e6-sitemap.schema.json; siteMap.ts gate validateE6SiteMap: coverage/partition, unique ids, actors/entity known, kind+workflowId DERIVED via deriveE6SiteMapKinds) writing the permanent l4/<module>/siteMap.defs.ts (absorbs navigation.defs.ts; workspaceIds index). PHASE 2 `e6-detail` — a parallel_dynamic fan-out (1 call per workspace, schemas/e6-workspace.schema.json) writing workspaces/<id>.defs.ts (sections/organisms/bffCalls + a sliceHash from computeE6WorkspaceSliceHash for incremental re-runs); detail gate = validateE6WorkspaceEquality (map owns id/title/actors/kind/operationIds) + a scoped validateE6Invariants for isolated bffCall fast-fail. `e6-finalize` (no LLM) reassembles map+details and runs the AUTHORITATIVE validateE6Invariants (unchanged — the 20 e6 tests stay valid) + a repair round for missing workspaces, then emits e6-done. prompt.md split into promptSiteMap.md (partition) + promptDetail.md (bffCalls/organisms). e7 + the N4 emitter rewired to read siteMap (fallback navigation). P8: journeys.ts derives l4/<module>/journeys/<journeyId>.defs.ts (narrative + operationIds via featureRefs + workspaceId via the map) with 3 gates (operationIds ⊆ ops; workspaceId in map; op unreferenced → warning), written at e6-finalize. New unit tests: siteMap.test.ts, journeys.test.ts. DEFERRED (flagged): the site-map JUDGE (adversarial LLM) and the HUMAN CHECKPOINT — additive quality layers on top of the working core; land tsc/unit-clean pending the A/B run.
- 2026-07-19 — command-form primarySurface (newSolution_16 failure "workspaces missing after repair round: reservationWorkspace"). A command-only page (a "create X" form whose only operation is a command — petShop customer `createReservation`, no query) could NOT satisfy the gate: primarySurface required a query dataSource, so the detail failed twice (`organism.reference.kind: primarySurface dataSource "createReservation" must be a query`) → never written → finalize reported it missing. Strong evidence it was the GATE, not the model: changeFrontend's `single_form` template (uxTemplates/single_form.defs.ts) has `slots.primarySurface: "commandForm"`, `appliesWhen.accessPatterns: ["commandInput"]`, `workspaceKinds: ["form", "entityManagement"]`, examples "Create a supplier / Create a new project" — the consumer explicitly renders a command-backed primarySurface. Fix (validateE6Invariants/validateOrganism): a `primarySurface` may be backed by a command via `action` (a form surface), in addition to a query via `dataSource` (a list surface). A command via `dataSource` is still an error (a list must be a query). promptDetail.md guides the LLM to use `action` for a command-only page's primarySurface. The 20 existing e6 tests are unchanged; a new test covers the form case.
- 2026-07-22 — run12 (102051 "workspaces missing after repair round: 5/6") — three systematic fixes + a LIVE detail test. (1) **repairE6BffFroms** (gate.ts, called in handleDetailResult/runE6Finalize before the gate): grok AND kimi systematically write RELATIVE projection paths — "$items.<col>" without the op prefix inside item.fields, "<op>.<array>.$items" as an array field's from, and the "$items" primary shorthand for a NON-primary array's columns (posWorkspace/menuManagement/shiftCommand failed `bff.output.from.unknownOp` identically across both repair rounds). All unambiguous when the call composes exactly ONE operation → qualified deterministically; composed calls and cross-op prefixes stay untouched for the gate. (2) **siteMap.actors.notCovering at PHASE 1**: kitchenQueue declared actors [cozinheiro] but hosted updateOrderStatus (defs actors also "atendente") — the detail copies actors verbatim from the slice, so the coverage hole could NEVER be fixed by detail retries (it burned every round). E6SiteMapGateContext gains `operationActors` (from the defs facts) and validateE6SiteMap enforces coverage where the retry CAN regroup/widen; summarizeOperationsLight now shows the FULL defs `actors` (was the single classification actorId — the model literally could not see the secondary actor). (3) **`sections` no longer schema-required** (e6-workspace.schema.json), same precedent as `purpose`: grok intermittently omits it even under forced tool choice, and the provider-side rejection (TOOL_ARGS_SCHEMA → 502, primary+fallback burned) is strictly worse than the existing `workspace.sections.empty` gate error + repair; promptDetail.md gains HARD REQUIREMENTS (sections required, never {} entries, full `from` grammar with WRONG/RIGHT examples). NEW e6DetailLive.test.ts: the phase-2 live test (the run12 failure was invisible to the suite — only the site map had a live test) replaying the REAL stockManagement/shiftCommand slices through the REAL detail pipeline; gate.test.ts replays the exact run12 relative-path payloads through repairE6BffFroms.
