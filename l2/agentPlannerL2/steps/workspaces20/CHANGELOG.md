# workspaces20

## 2026-09-18 (p2_02)

- Deterministic candidates from l4 journeys grouped by `(entity, actorRef, kind)`.
- LLM chooses and names among candidates (`submitP2Workspaces`). Gate + bounded repair.
- Draft `l2/<mod>/pipeline/workspaces20-draft.json`. Fixture: mensalidadesAcademia.
