# contracts30

## 2026-09-18 (p2_03)

- Field catalog per entity from `ontologyPaths` (type, derived, enum).
- Deterministic call slots from the workspace cut and journey steps.
- LLM names calls and selects catalog fields (`submitP2Contracts`). Gate + bounded repair.
- Emitter writes `web/contracts/{workspaceId}.defs.ts` (interfaces + route constants only).
- Fixture: mensalidadesAcademia.
